Place the supplied dataset here as `input.vcf.gz`.

Local configuration: `VCF_FILE=data/input.vcf.gz`.
Docker uses `/data/input.vcf.gz` inside the container.

The dataset is deliberately excluded from the source archive.
